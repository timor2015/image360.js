(function ($) {
	// 代码开始
    $.fn.image360 = function (options) {

    	// 插件的默认参数
        var defaults = {
		    
		};

		// 设定参数的覆盖顺序
		var settings = $.extend( {}, defaults, options);

		

    };
})(jQuery);